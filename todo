ssh-keygen -t rsa -b 4096 -C "tran.minhtam.tenshi@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
clip.exe < ~/.ssh/id_rsa.pub
add ssh key in github
sudo sed -Ei 's/^# deb-src /deb-src /' /etc/apt/sources.list; sudo apt-get update; sudo apt-get build-dep vim
cd /usr/local/src; git clone https://github.com/vim/vim.git
cd vim/src; sudo ./configure --with-features=huge --enable-multibyte --prefix=/usr/local --enable-python3interp=yes --with-python3-config-dir=$(python3-config --configdir); make install
sudo apt-get install golang; cd /usr/local/src; sudo git clone https://github.com/junegunn/fzf.git
cd fzf; make; make install
!git update-index --skip-worktree todo done achievements Session.vim tools/plantuml/plantuml.bat
take a break and celebrate
